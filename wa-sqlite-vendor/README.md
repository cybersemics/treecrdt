# @treecrdt/wa-sqlite-vendor

Workspace wrapper around the `wa-sqlite` git submodule used by TreeCRDT.

- `pnpm --filter @treecrdt/wa-sqlite-vendor build` builds `wa-sqlite/dist` (incremental).
- `pnpm --filter @treecrdt/wa-sqlite-vendor rebuild` does a clean rebuild.
