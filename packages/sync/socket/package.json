{
  "name": "@treecrdt/sync-socket",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm -C ../protocol run build && pnpm -C ../material/sqlite run build && pnpm run build && vitest run"
  },
  "dependencies": {
    "@treecrdt/discovery": "workspace:*",
    "@treecrdt/interface": "workspace:*",
    "@treecrdt/sync": "workspace:*",
    "@treecrdt/sync-sqlite": "workspace:*"
  },
  "devDependencies": {
    "@treecrdt/benchmark": "workspace:*",
    "@types/node": "^20.19.25",
    "typescript": "^5.9.3",
    "vitest": "^1.6.0"
  }
}
