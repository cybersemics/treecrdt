{
  "name": "@treecrdt/wa-sqlite-demo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build:vendor:wa-sqlite": "pnpm --filter @treecrdt/wa-sqlite-vendor build",
    "build:wa-sqlite": "pnpm run build:vendor:wa-sqlite && pnpm -C .. run build:ts",
    "build": "pnpm -C ../../treecrdt-sync run build && pnpm -C ../../treecrdt-sqlite-conformance run build && pnpm run build:wa-sqlite && vite build",
    "build:app:path": "vite build --base /base-path/",
    "build:path": "pnpm -C ../../treecrdt-sync run build && pnpm -C ../../treecrdt-sqlite-conformance run build && pnpm run build:wa-sqlite && vite build --base /base-path/",
    "preview:path": "vite preview --host --port 4172 --base /base-path/ --strictPort",
    "dev": "vite",
    "test:e2e": "pnpm -C ../../treecrdt-benchmark run build && pnpm -C ../../treecrdt-sync run build && pnpm -C ../../treecrdt-sqlite-conformance run build && pnpm run build:wa-sqlite && playwright test",
    "bench": "pnpm -C ../../treecrdt-benchmark run build && pnpm run build && playwright test tests/bench-opfs.spec.ts tests/bench-sync.spec.ts --reporter=line"
  },
  "dependencies": {
    "@treecrdt/benchmark": "workspace:*",
    "@treecrdt/sqlite-conformance": "workspace:*",
    "@treecrdt/sync": "workspace:*",
    "@treecrdt/wa-sqlite": "workspace:*",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@treecrdt/wa-sqlite-vendor": "workspace:*",
    "@types/node": "^20.19.25",
    "@types/react": "^18.3.27",
    "@types/react-dom": "^18.3.7",
    "@vitejs/plugin-react": "^4.7.0",
    "typescript": "^5.9.3",
    "vite": "^5.4.21"
  }
}
