{
  "name": "@treecrdt/sqlite-node",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "native"
  ],
  "scripts": {
    "build:native-ext": "cd ../.. && pnpm run build:ext:native",
    "copy:ext": "node ./scripts/copy-ext.mjs",
    "build:ts": "pnpm -C ../treecrdt-sqlite-conformance run build && tsc -p tsconfig.json",
    "build": "pnpm run build:native-ext && pnpm run copy:ext && pnpm run build:ts",
    "test": "pnpm -C ../treecrdt-sync run build && pnpm run build && vitest run",
    "benchmark": "pnpm -C ../treecrdt-sync run build && pnpm run build && tsx ./scripts/bench.ts && tsx ./scripts/bench-sync.ts"
  },
  "peerDependencies": {
    "better-sqlite3": "^9.0.0"
  },
  "dependencies": {
    "@treecrdt/interface": "workspace:*"
  },
  "devDependencies": {
    "@noble/ed25519": "^3.0.0",
    "@noble/hashes": "^1.8.0",
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^20.19.25",
    "@treecrdt/auth": "workspace:*",
    "@treecrdt/benchmark": "workspace:*",
    "@treecrdt/sqlite-conformance": "workspace:*",
    "@treecrdt/sync": "workspace:*",
    "better-sqlite3": "^9.0.0",
    "cborg": "^4.3.2",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "vitest": "^1.6.0"
  }
}
