{
  "name": "@justthrowaway/sqlite-node",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "native"
  ],
  "scripts": {
    "build:native-ext": "cd ../.. && pnpm run build:ext:native",
    "copy:ext": "node ./scripts/copy-ext.mjs",
    "build:ts": "tsc -p tsconfig.json",
    "build": "pnpm run build:native-ext && pnpm run copy:ext && pnpm run build:ts",
    "test": "pnpm -C ../sync-protocol/protocol run build && pnpm -C ../sync-protocol/material/sqlite run build && pnpm run build && vitest run",
    "benchmark:ops": "pnpm -C ../sync-protocol/protocol run build && pnpm -C ../sync-protocol/material/sqlite run build && pnpm run build && tsx ./scripts/bench.ts",
    "benchmark:note-paths": "pnpm -C ../treecrdt-benchmark run build && pnpm run build && tsx ./scripts/bench-note-paths.ts",
    "benchmark:sync": "pnpm -C ../treecrdt-benchmark run build && pnpm -C ../sync-protocol/protocol run build && pnpm -C ../sync-protocol/material/sqlite run build && pnpm -C ../treecrdt-postgres-napi run build && pnpm -C ../sync-protocol/server/postgres-node run build && pnpm run build && tsx ./scripts/bench-sync.ts",
    "benchmark": "pnpm run benchmark:ops && pnpm run benchmark:note-paths && pnpm run benchmark:sync"
  },
  "peerDependencies": {
    "better-sqlite3": "^9.0.0"
  },
  "dependencies": {
    "@justthrowaway/interface": "workspace:*",
    "@justthrowaway/sync-sqlite": "workspace:*"
  },
  "devDependencies": {
    "@noble/ed25519": "^3.0.0",
    "@noble/hashes": "^1.8.0",
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^20.19.25",
    "@justthrowaway/auth": "workspace:*",
    "@justthrowaway/benchmark": "workspace:*",
    "@justthrowaway/postgres-napi": "workspace:*",
    "@justthrowaway/engine-conformance": "workspace:*",
    "@justthrowaway/sync-protocol": "workspace:*",
    "@justthrowaway/sync-sqlite": "workspace:*",
    "@justthrowaway/sync": "workspace:*",
    "@justthrowaway/sync-server-postgres-node": "workspace:*",
    "better-sqlite3": "^9.0.0",
    "cborg": "^4.3.2",
    "@types/ws": "^8.18.1",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "vitest": "^1.6.0",
    "ws": "^8.18.3"
  }
}
