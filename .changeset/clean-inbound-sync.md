---
'@treecrdt/sync': minor
---

Add an inbound sync helper with aggregate per-target failures, deduplicated filters, cancelling
timeouts and stale peer work, live filter subscriptions, and terminal async teardown.
